<!----------フォロー中の会員・フォロワーの会員一覧-------------->

<div class="container">
<%= render partial: 'mypage_header', locals: { user: @user, page_user: @user } %>

<div class="row">
  <div class="col-md-11 wrap">

    <ul class="nav nav-tabs">
      <li class="nav-item">
        <a href="#tab1" class="nav-link" data-toggle="tab">フォロー中</a>
      </li>
      <li class="nav-item">
        <a href="#tab2" class="nav-link" data-toggle="tab">フォロワー</a>
      </li>
    </ul>

    <div class="tab-content p-4">
      <div id="tab1" class="tab-pane active">
        <%= render partial: 'following_table', locals: { followings: @followings, page_user: @user } %>
      </div>

      <div id="tab2" class="tab-pane">
        <%= render partial: 'follower_table', locals: { followers: @followers, page_user: @user } %>
      </div>
    </div>

  </div>
</div>

</div>