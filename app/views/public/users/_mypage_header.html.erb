<!-----------マイページ（会員詳細）ヘッダー部分--------------->

<div class="row d-flex mt-5">
  <div class="col-md-2 col-sm-1 text-md-right">
    <%= link_to (attachment_image_tag user, :profile_image, format: 'jpg', fallback: 'no_profile_image.jpg', size:'80x80', class:'rounded-circle'), user_path(user.id) %>
  </div>

  <div class="col-md-4 col-sm-4">
    <h3><%= link_to user.name, user_path(user.id), class:'jp-font-pop font-color-green' %></h3>
    <p class='jp-font-pop pr-1'><%= user.introduction %></p>
  </div>

  <div id="follow_count" class="col-md-3 offset-1 pb-4 mt-3">
    <%= render partial: 'follow_count', locals: { user: user } %>
  </div>

  <div class="col-md-2 col-sm-3 text-left">
    <% if user_signed_in? && user.id == current_user.id %>
      <%= link_to "レシピを書く", new_post_recipe_path, class: 'fas fa-edit btn btn-sm btn-warning text-white p-2' %><br/>
      <%= link_to "", edit_user_path(current_user.id), class: 'btn btn-outline-secondary btn-sm fas fa-user-cog px-4 mt-3' %>
    <% else %>
      <div id="header-follow-btn" class="mt-3 text-left ml">
        <%= render partial: 'follow_button', locals: { user: user, page_user: page_user } %>
      </div>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col-12">
    <hr class="border-bottom">
  </div>
</div>