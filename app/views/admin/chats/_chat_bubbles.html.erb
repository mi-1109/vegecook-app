<!----------- 管理者側：チャット内容の部分テンプレート -------------->

  <div class="row mt-3 mb-5">
    <div class="chat-area mx-auto">
      <% @chats.each do |chat| %>
        <!--管理者(admin)のチャット内容-->
        <% if chat.admin_id != nil %>
          <div class="text-right mt-4">
            <p style="font-size: 11px" class="mb-0 mr-2">
              <%= l chat.created_at %>
              <span class="ml-2 font-weight-bold">VegeCook事務局</span>
            </p>
            <div class="chat-bubble text-left">
              <%= chat.message %>
            </div>
          </div>
        <!--顧客(user)のチャット内容-->
        <% elsif chat.user_id == user.id %>
          <div class="mt-4">
            <p style="font-size: 11px" class="mb-0 ml-md-2 ml-sm-0">
              <span class="ml-md-3 ml-sm-1 font-weight-bold"><%= chat.user.name %></span>
              <span class="ml-3"><%= l chat.created_at %></span>
            </p>
            <div class="chat-bubble receiver-chat-bubble">
              <%= chat.message %>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>